export const PROPERTY_TYPES_VALUES = [
  "STRING",
  "INTEGER",
  "BOOLEAN",
  "OBJECT",
  "ARRAY",
] as const;

export enum PROPERTY_TYPES {
  STRING = "STRING",
  INTEGER = "INTEGER",
  BOOLEAN = "BOOLEAN",
  OBJECT = "OBJECT",
  ARRAY = "ARRAY",
}
