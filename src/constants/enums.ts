export const PROPERTY_TYPES_VALUES = [
  "STRING",
  "NUMBER",
  "BOOLEAN",
  "OBJECT",
  // "ARRAY",
] as const;

export enum PROPERTY_TYPES {
  STRING = "STRING",
  NUMBER = "NUMBER",
  BOOLEAN = "BOOLEAN",
  OBJECT = "OBJECT",
  // ARRAY = "ARRAY",
}
